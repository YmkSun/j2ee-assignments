Create table registrationDB(
sname varchar(20),
email varchar(30),
address varchar(40), 
contactNo varchar(15),
CONSTRAINT registrationdb_email_pk PRIMARY KEY (email)
);

Select * from registrationDB;
