drop table userProfile;

Create table userProfile(
userid varchar(20),
username varchar(20),
password varchar(20),
email varchar(25),
address varchar(30),
CONSTRAINT userprofile_userid_pk PRIMARY KEY (userid)
);

select * from userProfile;